# this is for writing test driven developement tests
# severspec is like rspec but for sysadmins

# the gemfile cointains all our dependencies inside a test a group. This is for ease of deployment
source "https://rubygems.org"

group :test do
    gem 'serverspec'
    gem 'docker-api'
    end

